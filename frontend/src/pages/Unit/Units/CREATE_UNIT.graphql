mutation CreateUnit($parentUnitId: ID!, $unit: UnitInput!) {
    createUnit(parentUnitId: $parentUnitId, unit: $unit) {
        _id
        name
        shortName
        head {
            _id
        }
    }
}